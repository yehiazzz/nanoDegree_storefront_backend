CREATE TABLE users(
                        id SERIAL PRIMARY KEY, 
                        first_name VARCHAR(20), 
                        last_name VARCHAR(20), 
                        user_password VARCHAR
                    );